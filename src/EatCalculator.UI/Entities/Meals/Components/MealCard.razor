@inherits BaseComponent

<div class="@GetCssName(nameof(MealCard)) border rounded w-100 mt-2 @ClassName"
     style="@StyleName"
@attributes="UserAttributes">
    <EcCard Title="@Meal.Title"
            OnClick="OnClick">
        <OptionalContent>
            @OptionalContent
        </OptionalContent>
        <BottomContent>
            @BottomContent
        </BottomContent>
    </EcCard>
</div>
