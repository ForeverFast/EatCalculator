@namespace UI

@inherits ComponentBase

@typeparam T

<Condition Evaluation="@_canIterateItems">
    <Match>
        @foreach (T item in Items!)
        {
            @ChildContent!.Invoke(item)
        }
    </Match>
</Condition>
