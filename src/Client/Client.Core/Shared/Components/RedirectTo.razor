@namespace Client.Core.Shared.Components

@inject NavigationManager _navigationManager

@code {
    [Parameter] public string Uri { get; set; } = "/";

    protected override void OnInitialized() => _navigationManager.NavigateTo(Uri);
}
